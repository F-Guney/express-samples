doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{title}
        link(rel="stylesheet", href="/css/main.css")
        //- Additional CSS files included as below:
        //- block styles
    body 
        header
            a(href="/form") Add User
            br
            a(href="/users") Users
        main
            block content